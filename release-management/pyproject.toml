[project]
name = "ceph-fork-release-management"
version = "0.0.0"
description = "Release-management scripts shipped into the Ceph downstream fork. Self-contained — no external tooling checkout at CI time."
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.21",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
