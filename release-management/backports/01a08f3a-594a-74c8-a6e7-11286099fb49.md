---
id: 01a08f3a-594a-74c8-a6e7-11286099fb49
title: Fix Swift ACL evaluation for Glance image sharing
component: rgw
category: fix
provenance:
  type: upstream_pr
  upstream_prs:
  - https://github.com/ceph/ceph/pull/68795
    https://github.com/ceph/ceph/pull/70997
  upstream_pr_state: open-in-review
  upstream_commits:
  - 0a96b7d44d90126a656575e53629565fcdd9d58b
  - 68d8b416a6a6618be9799955673cb0c0722b18c5
  upstream_trackers:
  - https://tracker.ceph.com/issues/68492
    https://tracker.ceph.com/issues/68476
justification:
  type: customer
  refs:
  - cobaltcore-dev/cloud-storage#570
  - cobaltcore-dev/cloud-storage#477
risk:
  # Stage-B — fill these during PR prep. See release-management/backports/RISK-RUBRIC.md.
  blast: availability
  conflict: clean
  coverage: strong
---

## Justification (public)

Fix Swift ACL evaluation with Keystone so Glance images stored in a multi-tenant Swift backend can be shared across projects. Without this fix, accepted shared images are visible to destination projects, but downloading them returns an empty file and fails checksum validation.

## Justification (internal)

Glance metadata sharing works, and the image object remains valid and readable by its owner. However, RGW returns AccessDenied when the destination project accesses the object, despite the appropriate Swift container ACL.

The fix synchronizes Keystone ACL evaluation between the Swift and S3 paths and supports the authenticated `*:*` wildcard. The solution was validated on st1-eu-de-3. A Squid backport is available as ceph/ceph#70997.

## Risk notes

Authorization behavior is affected, but only for Keystone-authenticated RGW requests using Swift/S3 ACL interoperability. The backport is clean with no data structure change and includes focused unit coverage plus validation in a Swift/Glance environment.

## Behavior change

Authenticated Keystone users can now access containers granted through *:*. S3 requests authenticated through Keystone also honor Swift-style project:user and wildcard ACL entries. Bare role-name ACLs remain intentionally unsupported.

## Upgrade notes

No configuration, data migration, restart sequencing, or ACL rewrite is needed. Deploy the updated RGW packages and restart/roll the RGW daemons according to the normal upgrade procedure. Existing ACLs take effect automatically.
